import {createTheme} from "@mui/material";

const theme = createTheme({
   palette: {
      mode: 'light',
      primary: {
         main: '#007035'
      },
      secondary: {
         main: '#BDD0B8FF'
      },
      background: {
         default: '#f8f8f8',
      },
   }
});

export default theme;
